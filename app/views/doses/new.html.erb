<div class="container">
  <%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.association :ingredient %>
    <%= f.input :description %>
    <%= f.button :submit %>
  <% end %>

<br>
<br>
<%= link_to 'cancel', cocktail_path(@cocktail) %>
</div>
